\section{ Prompt Construction for CVM}
  \begin{enumerate}

      \item Input : Submitted Python code \textcolor{green}{[EXPLICIT]}

      \item Output : CVM validation prompt for GPT-4o \textcolor{blue}{[INFERRED]}

      \item Tool/Method/Algorithm : LangChain \textcolor{green}{[EXPLICIT]}

      \item Process :  LangChain fills template with code and adds validation instructions, system role. \textcolor{blue}{[INFERRED]}. Authors created these templates in the prompt design phase. 
  \end{enumerate}


\section{Code Validation (CVM) using LLM}
  \begin{enumerate}
      \item Input: CVM prompt for GPT-4o \textcolor{blue}{[INFERRED]}
      \item Output: Valid/invalid Python flag \textcolor{blue}{[INFERRED]}
      \item Tool: GPT-4o \textcolor{green}{[EXPLICIT]}
      \item Process: GPT-4o checks if code is valid Python program \textcolor{green}{[EXPLICIT]}
  \end{enumerate}

\section{Answer Check (Server-side)}
    \begin{enumerate}
        \item Input: Valid Python code + test cases \textcolor{blue}{[INFERRED]}
        \item Output: Pass/fail results + syntax errors \textcolor{green}{[EXPLICIT]}
        \item Tool: Server-side test execution \textcolor{green}{[EXPLICIT]}
        \item Process:  Runs test cases and checks compilation errors \textcolor{green}{[EXPLICIT]}. If passes → Strict Code Check \textcolor{blue}{[INFERRED]}
    \end{enumerate}
