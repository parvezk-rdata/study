latexmk -C && latexmk -pdf -interaction=nonstopmode main.tex


Run biber main after the first LaTeX run.

-----------------------------------------------------------------

latexmk -C          # clean previous builds
latexmk -pdf main   # first pass, builds aux files
biber main          # generate bibliography
latexmk -pdf main   # rebuild with citations and references


-----------------------------------------------------------------------------------