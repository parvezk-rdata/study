\section{ Load Documents }
    \begin{enumerate}
        \item Load : Directory(multiple files).
        \item Load : plain text
        \item Load : pdf
        \item TextLoader creates one Document for entire file.
        \item PDFLoader creates one Document for each page.
        \item Loading pdf files can be noisy, unpredictable. PDF cleaning is mandatory later.
    \end{enumerate}
    \lstinputlisting[language=Python]{\subfix{sections/programs/prog_07.py}}
